# poopSite

зависимости
```bash
npm install
```

```bash
npm run build

./start-ngrok.sh
```

изменить порт (добавить флаг в package.json)
```json
"scripts": {
    "start": "webpack serve --mode development --port 9000",
}
```

разработка
```bash
npm start
```
